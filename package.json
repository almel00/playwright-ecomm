{
  "name": "abc-senior-living-ecomm-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright automation for ABC Senior Living ecomm flows.",
  "scripts": {
    "test": "playwright test",
    "test:resident-ordering": "playwright test tests/resident-ordering.spec.ts",
    "test:resident-ordering:debug": "playwright test tests/resident-ordering.spec.ts --debug",
    "test:coverage": "playwright test tests/auth.spec.ts tests/menu-coverage.spec.ts",
    "test:all": "playwright test",
    "test:profile": "playwright test",
    "report": "node scripts/show-latest-report.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "playwright": "^1.57.0"
  }
}
